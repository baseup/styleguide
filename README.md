# BaseUp Style Guide

### Requirements
* `nodejs` and `npm`
* `bower`

### Installation
Run `$ npm install` and `$ bower install`

### Start
Run `$ gulp`

### Generate a page
Run `$ gulp page --name <PAGE_NAME>`
